import 'package:flutter/material.dart';

class buttonCloneLanjut extends StatelessWidget {
  const buttonCloneLanjut({super.key});

  @override
  Widget build(BuildContext context) {
    return SizedBox(
      height: 50,
      child: ElevatedButton(
        onPressed: () {}, child: Text("Lanjut"),
      ),
    );
  }
}
